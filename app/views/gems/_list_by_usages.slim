table
  tr
    th Name
    th Versions
  - gem_usages.each do |gem_usage|
    - info = gem_usage.gem_info
    tr
      td
        = link_to info.name, gem_path(info)
      td= gem_usage.gem_version.version
